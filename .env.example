# API Configuration
# Set VITE_USE_DIRECT_API=true to test CORS configuration (direct backend access)
# Set VITE_USE_DIRECT_API=false to use Vite proxy (default, recommended for development)
VITE_USE_DIRECT_API=false

# Backend API URL (used in production or when VITE_USE_DIRECT_API=true)
VITE_API_BASE_URL=http://localhost:9001